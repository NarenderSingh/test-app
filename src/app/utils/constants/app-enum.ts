export const MODAL_STYLE = {
  POSITION: 1,
};

export enum ROLES {
  ADMIN = 1,
  MANAGER = 2,
  USER = 3,
}

export enum SEARCH_TYPE {
  USER = "user",
  INVITE = "invite",
}

export enum COLOR_CODE {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  SUCCESS = "success",
  DANGER = "danger",
}
